./docs/legacy/analise.md:90:- **Problema:** `<e-mail pessoal do mantenedor>` ocupa ~280px no header, empurrando os botões de Notificação e Sair para a borda direita.
./docs/auditoria/artefatos/G1-creds.txt:1:./docs/legacy/analise.md:90:- **Problema:** `<e-mail pessoal do mantenedor>` ocupa ~280px no header, empurrando os botões de Notificação e Sair para a borda direita.
./frontend/e2e/auth.spec.ts:37:    await page.getByLabel(/e-mail/i).fill("<e-mail pessoal do mantenedor>");
./frontend/e2e/auth.spec.ts:38:    await page.getByLabel(/senha/i).fill("[REDIGIDO]");
./docs/auditoria/plano.md:423:- [ ] **CRÍTICO 🔴**: `frontend/e2e/auth.spec.ts:40` contém senha real `[REDIGIDO]` e email `<e-mail pessoal do mantenedor>`. **Trocar a senha no app + remover do código + reescrever histórico** (ou ao menos remover do HEAD e trocar a senha).
./docs/auditoria/runbook.md:1185:rg -n "[REDIGIDO]|<e-mail pessoal do mantenedor>" \
=== Histórico git do auth.spec.ts ===
Author: Gabriel <<e-mail pessoal do mantenedor>>
Author: Gabriel <<e-mail pessoal do mantenedor>>
+    await page.getByLabel(/e-mail/i).fill("<e-mail pessoal do mantenedor>");
+    await page.getByLabel(/senha/i).fill("[REDIGIDO]");
Author: Gabriel <<e-mail pessoal do mantenedor>>
Author: Gabriel <<e-mail pessoal do mantenedor>>
